-- Deprecated duplicate migration kept as a no-op so Supabase migration ordering
-- remains stable in this workspace. Use `20260417140000_init.sql` as the
-- canonical schema and online API contract for the platform.
select 1;
