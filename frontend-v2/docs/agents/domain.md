# Domain Docs

Before exploring domain behavior, read `CONTEXT.md` or the relevant entries in `CONTEXT-MAP.md`, plus applicable ADRs under `docs/adr/`. If these files do not exist, proceed without inventing them.

Use the glossary's established vocabulary in code, tests, tickets, and documentation. If a proposed change contradicts an ADR, surface the conflict explicitly instead of silently overriding it.
