dist
coverage

# Vendored skill content — not house style, don't reformat someone else's docs.
.claude
.agents

# Vendored/regenerated by `npx shadcn add` — not house style, don't reformat
# library internals we don't hand-maintain (see eslint.config.js for the
# matching lint exemption).
src/components/ui
